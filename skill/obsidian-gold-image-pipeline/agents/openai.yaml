interface:
  display_name: "Obsidian Gold Image Pipeline"
  short_description: "Create or repair one Obsidian Gold object from a visual reference."
  default_prompt: "Create or repair one isolated Obsidian Gold object from the attached reference and deliver the validated image."
policy:
  allow_implicit_invocation: true
